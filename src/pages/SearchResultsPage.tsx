export function SearchResultsPage() {
  // TODO: read destination/dates from URL search params, fetch hotels via
  // features/hotels/api.ts, render filter sidebar + hotel result cards.
  return (
    <section className="mx-auto max-w-6xl px-6 py-12">
      <h1 className="text-2xl font-semibold">Search results</h1>
    </section>
  )
}
